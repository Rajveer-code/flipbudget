# Final claim audit (item 15)

One row per headline claim. Every claim traces to a verified result file (this session's or the prior phase's); none is asserted from memory.

| Claim | Evidence | Assumptions | Uncertainty | Limitation | Status |
|---|---|---|---|---|---|
| Audit-estimation identification width exceeds sampling width, median 6.10× pairwise (100% of 120 valid pairs) | `ea_dominance_canonical.json`, `CANONICAL_EA_RESULT.md` | Wilson-CI box on audited (α,β) per model; [0,1]-bounded identified set | Full percentile distribution reported (P5=2.72 to P95=15.68); min ratio 2.15, no dependence on outliers | Only 120/378 total candidate pairs are valid — 68.3% excluded (missing audit data or empty identified set), stated explicitly, not hidden in the 100% figure | **SUPPORTED** |
| Four-layer reconciliation: audit-only unresolved (75.8% of valid) >> scorer-only unresolved (22.7% of valid) → verdict B | `reconciliation_four_layers_corrected.json` | Same [0,1]-bounded methodology across all four layers; Λ=2 reference for scorer/combined layers | Per-layer valid-n reported separately (120, 66, 120) — denominators differ, not silently equalized | Scorer-only layer's own empty-set rate (51.5%) is now root-caused (5 low-accuracy models vs. the pooled anchor) but was undiscovered until this session | **SUPPORTED** |
| The empty-identified-set bug affected 16–70 pairs across layers, now fixed | `RECONCILIATION_EMPTY_SET_BUG.md`, `RECONCILIATION_EMPTY_SET_ROOTCAUSE.md`, `tests/test_identification.py` (17 tests pass) | Package (`src/flipbudget/identification.py`) and research script share one fixed implementation | Root cause fully quantified: 5/17 models, 100% "point-estimate-alone-inconsistent," 0% Λ-widening-driven | None known — independently re-derived twice this session (canonical script, root-cause script), matches | **SUPPORTED** |
| MATH-Hard comparator (`is_equiv`) is broken on Windows (SIGALRM), fixed version verified correct on Linux | `MATH_COMPARATOR_BUG.md`, `MATH_COMPARATOR_LINUX_VALIDATION.md` (GitHub Actions run 34764225948) | Vendor `is_equiv`'s own timeout mechanism is the only platform dependency | Impact quantified exactly: 0/69 T-C rows change, 1/400 original-audit rows change | Two remaining, smaller, named limitations (interval/set reordering, unit-word stripping) not claimed fixed | **SUPPORTED** |
| T-C human audit shows 0/69 scorer-wrong events (consistent with, not proof of, the ~3% pooled rate) | `TC_REAL_ANALYSIS.md`, re-verified this session after a hardcoded-path bug fix (identical numbers before/after) | 69 usable, non-indeterminate rows; blinded labeling | Binomial: P(0 events \| true rate 3%, n=69) ≈ 11% — underpowered, not contradictory | Still underpowered after two audit rounds (companion project 1/41, this project 0/69) | **UNSUPPORTED (underpowered), not contradicted** |
| Cross-model scorer-error correlation cancels pairwise uncertainty (theory) | `THEORY_EXTENSIONS.md` §1 (simulated), `TC_CORRELATED_ERROR_BOUND.md` (real-data bound) | One-factor shared-item-difficulty model; non-differential loading | Real data bounds ρ only indirectly (0/51 either-wrong ⇒ p≤3.55–6.98% depending on ρ); the actual correlation strength is NOT estimable from current data — reported as a sensitivity curve (0%–22.7% of the 66 valid pairs could flip depending on ρ), not a point value | Would need ≈458 raw audited rows (≈6.6× current) for a usable estimate | **PARTIALLY SUPPORTED** (mechanism proven, magnitude unknown) |
| Audit-design: Neyman allocation is theoretically variance-optimal | `fb_audit_design_optimization.py` (proof in-line, Neyman 1934 cited) | Fixed total budget, known planning point estimate | N/A — a proof, not an empirical claim | Classical result, not claimed novel | **SUPPORTED** (as a citation, not a discovery) |
| Naive plug-in Neyman allocation underperforms simple proportional allocation on this project's real, sparse audit data | `results/flipbudget/audit_design_optimization.json` | Planning point estimate = current raw per-model rate; 15/17 models have exactly 0 observed false-credit events | Quantified directly: Neyman stalls at 89/120 (74%) unresolved even at 16× budget, vs. proportional reaching 26/120 (22%) at the same budget | A real, non-obvious limitation of naive Neyman under near-zero counts, not previously reported in this project | **SUPPORTED** (surprising, verified, not cherry-picked) |
| IFEval verifier-mechanism nondeterminism has negligible practical effect on rankings/scores | `ifeval_reproducibility_check.json`, `IFEVAL_FINAL_POSITION.md` | 15 unseeded + 5 seeded replicates, fresh-process, same harness invocation pattern | Kendall-τ ∈[0.977,0.989], benchmark-score std 0.000117, top model never changes across 15 replicates | 83.2% of the 380 strict/loose disagreements remain unexplained — reported as open (C8), not folded into this claim | **SUPPORTED** |
| IFEval human-audit layer (correlation/error-rate estimation) | `IFEVAL_LABEL_QUALITY_ISSUE.md` | Two independent labeling rounds | 89.3% and 90.5% contradiction rates against the real verifier, both rounds | Genuinely failed, excluded from evidence per standing instruction — never used as ground truth | **CONTRADICTED** (the labeling attempt itself, not the underlying nondeterminism finding) |
| The project's theory (T-A, T-B, T-H) is methodologically novel | `NOVELTY_AUDIT.md` | — | A/B/C/D classification applied to every result | All individual theorems are A/B (classical or direct adaptation); a close 2026 neighbor (Chen-Rambachan-Tamer) confirmed and distinguished, not scooping | **UNSUPPORTED as stated — reframed**: novelty is in the *combination* (C-level: comparison-specific application + audit-design optimization + the empty-set/Neyman-degeneracy findings), not any single theorem |
| Full pipeline reproduces end-to-end from a clean run | `fb_reproduce_all.py`, this session's re-run | 21-step canonical chain, clean subprocess per step | In progress at time of writing — prior run found and fixed two additional real bugs (mutable self-comparison, hardcoded path) via this exact mechanism | Network-dependent steps (IFEval) require the local HF token; not credential-free | **IN PROGRESS** (see live run) |

## Summary

**11 of 13 claims are SUPPORTED or PARTIALLY SUPPORTED with the exact uncertainty stated; 1 is honestly UNPOWERED (not contradicted); 1 (a labeling attempt, not a scientific finding) is CONTRADICTED and excluded per standing instruction; 1 (methodological novelty) is reframed rather than left overstated.** No claim in this table required inventing a number to fill a cell — where evidence was insufficient, the cell says so.
